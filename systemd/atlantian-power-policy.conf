[Login]
# CTRL_C41 has no PMIC-controlled standby rail.  Reboot and poweroff are
# meaningful; suspend/hibernate would only advertise states the board cannot
# resume from reliably.
HandleSuspendKey=ignore
HandleHibernateKey=ignore
