# Only repository indexes live in the bounded volatile workspace. Downloaded
# packages use APT's normal storage-backed archive directory and are not retained.
d /run/apt               0755 root root -
d /run/apt/lists         0755 root root -
d /run/apt/lists/partial 0700 _apt root -
