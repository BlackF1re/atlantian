// AtlANTian keeps repository indexes volatile without forcing downloaded .deb
// payloads into RAM. Lists live in the bounded /run/apt tmpfs; package archives
// use APT's normal storage-backed cache and are discarded after installation.
Dir::State::Lists "/run/apt/lists/";
APT::Keep-Downloaded-Packages "false";

// Rebuild these disposable structures on demand instead of storing cache files.
Dir::Cache::pkgcache "";
Dir::Cache::srcpkgcache "";

// Keep repository metadata compact and omit data unused by normal package work.
Acquire::Languages "none";
Acquire::GzipIndexes "true";
Acquire::CompressionTypes::Order { "gz"; };
Acquire::IndexTargets::deb::Contents-deb::DefaultEnabled "false";
Acquire::IndexTargets::deb::Contents-udeb::DefaultEnabled "false";
Acquire::IndexTargets::deb-src::Contents-dsc::DefaultEnabled "false";
